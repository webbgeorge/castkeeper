package castkeeper

// Version is a short commit hash, set with ldflags at build time
// e.g. `go build -ldflags='-X "github.com/webbgeorge/castkeeper.Version={SOME_COMMIT_HASH}"' cmd/server/main.go`
var Version string
